services:
  miniflux:
    image: ghcr.io/miniflux/miniflux:latest
    container_name: miniflux
    restart: unless-stopped
    ports:
      - "8002:8080"
    env_file:
      - .env
    environment:
      - RUN_MIGRATIONS=1
      - CREATE_ADMIN=1
